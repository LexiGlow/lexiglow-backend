openapi: 3.0.3
info:
  title: LexiGlow Backend (skeleton)
  version: "0.1.0"
servers:
  - url: /
paths:
  /health:
    get:
      summary: Health check
      operationId: app.controllers.health.get_health
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
